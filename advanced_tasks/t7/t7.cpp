/*
Напишите  программу,  которая  читает  целое  значение  с  терминала  и  посылает  это
значение всем MPI–процессам. Каждый процесс должен печатать свой  номер и полученное
значение. Значения следует читать, пока не появится на входе отрицательное целое число.
В МPI базисной операцией посылки является операция:

*/
#include <mpi.h>
#include <iostream>

int main(int argc, char const *argv[])
{
    return 0;
}
